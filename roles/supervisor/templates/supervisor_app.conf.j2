[program:{{ item.name }}]
command=/usr/local/bin/pipenv run python {{projects_workdir}}/{{ item.name }}/{{ item.app_exec }}
directory={{projects_workdir}}/{{ item.name }}
user={{item.app_user}}
autostart=true
autorestart=true
redirect_stderr=true

